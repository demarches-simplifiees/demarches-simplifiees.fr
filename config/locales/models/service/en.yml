en:
  activerecord:
    models:
      service:
        one: 'Service'
        other: 'Services'
    attributes:
      service:
        siret: "TAHITI number"
        adresse: 'Mail address'
        email: 'Email'
        telephone: 'Phone'
    errors:
      models:
        service:
          attributes:
            siret:
              format: 'must start with lettee or digit, followed by 5 digits'
              invalid: "doesn't point to a valid firm"
              checksum: 'has error, verify the number'
