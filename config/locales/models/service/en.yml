en:
  activerecord:
    models:
      service:
        one: 'Service'
        other: 'Services'
    attributes:
      service:
        siret: "TAHITI number"
        adresse: 'Mail address'
        email: 'Email'
        telephone: 'Phone'
    errors:
      models:
        service:
          attributes:
            siret:
              format: "TAHITI number %{message}"
              length: "must start with lettee or digit, followed by 5 digits"
              checksum: "is invalid"
