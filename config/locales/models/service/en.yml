en:
  activerecord:
    models:
      service:
        one: 'Service'
        other: 'Services'
    attributes:
      service:
        adresse: 'Mail address'
        email: 'Email'
        telephone: 'Phone'
        nom: Service name
        organisme: Organism(s)
        siret: TAHITI number
    errors:
      models:
        service:
          attributes:
            siret:
              format: "TAHITI number %{message}"
              length: "must start with lettee or digit, followed by 5 digits"
              checksum: "is invalid"
