en:
  activerecord:
    errors:
      models:
        champs/formatted_champ:
          attributes:
            value:
              invalid_regexp: '%{expression_reguliere_error_message}'
              letters_forbidden: 'must not contain letters'
              numbers_forbidden: 'must not contain numbers'
              special_characters_forbidden: 'must not contain special characters'
              min_character_length_rule: 'must be at least %{min} characters'
              max_character_length_rule: 'must be less than %{max} characters'
