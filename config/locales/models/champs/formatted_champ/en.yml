en:
  activerecord:
    errors:
      models:
        champs/formatted_champ:
          attributes:
            value:
              invalid_regexp: '%{expression_reguliere_error_message}'
              letters_forbidden: "Letters forbidden"
              numbers_forbidden: 'Numbers forbidden'
              special_characters_forbidden: 'Special characters forbidden'
              min_character_length_rule: "Character length must be greater than %{min}"
              max_character_length_rule: "Character length must be less than %{max}"
