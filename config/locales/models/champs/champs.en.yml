en:
  activerecord:
    errors:
      models:
        champ:
          attributes:
            value:
              format: '%{message}'
              missing: must be filled
              invalid_regexp: '%{expression_reguliere_error_message}'
