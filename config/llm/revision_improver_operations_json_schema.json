{
  "type": "object",
  "definitions": {
    "champ_type": {
      "type": "string",
      "enum": [
        "header_section",
        "repetition",
        "explication",
        "civilite",
        "email",
        "phone",
        "address",
        "text",
        "textarea",
        "date",
        "piece_justificative",
        "titre_identite",
        "checkbox",
        "yes_no",
        "drop_down_list",
        "multiple_drop_down_list"
      ]
    },
    "champ_properties": {
      "type": "object",
      "properties": {
        "type_champ": { "$ref": "#/definitions/champ_type" },
        "libelle": { "type": "string" },
        "description": { "type": "string" },
        "mandatory": { "type": "boolean", "default": true },
        "children": {
          "type": "array",
          "items": { "$ref": "#/definitions/champ_properties" }
        }
      },
      "required": ["type_champ", "libelle"],
      "allOf": [
        {
          "if": {
            "properties": { "type_champ": { "const": "repetition" } }
          },
          "then": {
            "required": ["children"]
          },
          "else": {
            "not": { "required": ["children"] }
          }
        }
      ]
    }
  },
  "properties": {
    "summary": {
      "type": "string",
      "description": "Summary of important modifications suggested"
    },
    "operations": {
      "type": "object",
      "properties": {
        "destroy": {
          "type": "array",
          "items": { "type": "integer" }
        },
        "update": {
          "type": "array",
          "items": {
            "allOf": [
              { "$ref": "#/definitions/champ_properties" },
              {
                "properties": {
                  "stable_id": { "type": "integer" }
                },
                "required": ["stable_id"]
              }
            ]
          }
        },
        "add": {
          "type": "array",
          "items": {
            "allOf": [
              { "$ref": "#/definitions/champ_properties" },
              {
                "properties": {
                  "after_stable_id": { "type": "integer" }
                },
                "required": ["after_stable_id"]
              }
            ]
          }
        }
      },
      "additionalProperties": false
    }
  },
  "required": ["summary", "operations"],
  "additionalProperties": false
}
