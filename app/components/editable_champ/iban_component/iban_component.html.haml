= @form.text_field(:value, input_opts(id: @champ.input_id, required: @champ.required?, aria: { labelledby: labelledby_id, describedby: "#{@champ.describedby_id} #{@champ.error_id}" }, data: { controller: 'format', format: 'iban' }, class: "width-66-desktop", maxlength: 34 + 9, size: nil)) # count space separator of 4 digits-groups
