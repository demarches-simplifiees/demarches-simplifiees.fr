- class_names = 'header-section ' + (@champ.dossier.auto_numbering_section_headers_for?(@champ) ? "header-section-counter" : "")
= content_tag "h" + (1+@champ.level).to_s, { class: class_names } do
  = @champ.libelle
