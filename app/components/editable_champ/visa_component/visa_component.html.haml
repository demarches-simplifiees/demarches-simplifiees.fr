.row
  = @form.check_box :value,
    { required: @champ.required?,
      id: @champ.input_id,
      disabled: !@champ.user_accredited?(helpers.current_user),
      checked: @champ.value.present?,
      aria: { describedby: @champ.describedby_id },
      data: { controller: 'visa' } },
    @champ.value.presence || helpers.current_user&.email || '1',
    ''
  .visa-info
    - if @champ.value.present?
      validé par
      = @champ.value
    - else
      cochez la case pour valider.
