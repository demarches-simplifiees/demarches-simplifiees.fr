.champ-row
  %dt.champ-label
    = "#{label}"
    = render Dossiers::ChampUpdatedComponent.new(updated_at:, seen_at:)

  - if blank?
    %dd.champ-content.fr-text-mention--grey{ class: content_class }
      - if usager?
        %p.champ-blank= blank

  - else
    %dd.champ-content.copy-zone{ class: content_class }
      = value
