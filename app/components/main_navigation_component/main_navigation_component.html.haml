%nav#header-navigation.fr-nav{ role: 'navigation', 'aria-label': 'Menu principal gestionnaire' }
  %ul.fr-nav__list
    - if !controller_path.include?('super_admin')
      = render MainNavigation::BackLinkComponent.new
      = render MainNavigation::AdministrateurLinkComponent.new
      = render MainNavigation::InstructeurLinkComponent.new
      = render MainNavigation::ExpertLinkComponent.new
      = render MainNavigation::GestionnaireLinkComponent.new
      = render MainNavigation::UsagerLinkComponent.new
      = render MainNavigation::AnnouncesLinkComponent.new
    - else
      %li.fr-nav__item
        = link_to("Nouvelle annonce", new_super_admins_release_note_path(date: @release_note&.released_on), class: "fr-nav__link", target: "_self", aria: { current: action_name == :new ? "page" : nil })
      %li.fr-nav__item
        = link_to "Annonce", '', class: "fr-nav__link", target: "_self", aria: { current: "page" }
      %li.fr-nav__item
        = link_to "Annonces publiées", release_notes_path, class: "fr-nav__link", target: "_self"
