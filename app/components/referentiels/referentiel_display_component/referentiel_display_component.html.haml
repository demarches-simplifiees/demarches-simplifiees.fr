.fr-table
  .fr-table__wrapper
    .fr-table__container
      .fr-table__content
        %table
          %caption.fr-text--regular.fr-text--sm Indiquez les données issues du référentiel que vous sohaitez voir affichées pour l'usager et/ou l'instructeur
          %thead
            %tr
              %th{ scope: "col" } Propriété
              %th{ scope: "col" } Exemple de donnée
              %th{ scope: "col" } Type de donnée
              %th{ scope: "col" } Libellé de la donnée récupérée
              %th{ scope: "col" } Afficher à l’usager
              %th{ scope: "col" } Afficher à l’instructeur
          %tbody
            - display_mapping.sort.each do |jsonpath, mapping_opts|
              %tr
              %td= jsonpath
              %td.fr-cell--multiline= mapping_opts[:example_value] || mapping_opts["example_value"]
              %td= mapping_opts[:type] || mapping_opts["type"]
              %td= mapping_opts[:libelle] || mapping_opts["libelle"]
              %td.text-center= display_tag_for(jsonpath, "display_usager")
              %td.text-center= display_tag_for(jsonpath, "display_instructeur")
