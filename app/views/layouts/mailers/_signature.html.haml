%p
  Bonne journée,
  %br
  - if defined?(service) && service && service.nom.present?
    = service.nom
  - else
    L'équipe #{SITE_NAME}
    %br
    Direction de la Modernisation et des Réformes de l'Administration / DMRA
    Service Informatique de la Polynésie Française / SIPf
