- content_for(:title, "Traces horodatées · Dossier nº #{@dossier.id} (#{@dossier.owner_name})")

= render partial: "header", locals: { dossier: @dossier }

= render partial: "shared/dossiers/operations_logs", locals: { dossier: @dossier }
