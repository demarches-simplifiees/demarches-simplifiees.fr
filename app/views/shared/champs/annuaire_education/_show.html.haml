- if champ.data.blank?
  %span.copy-btn{ role: "button" }= champ.to_s
- else
  %table.table.vertical.dossier-champs
    %tbody
      %tr
        %th.libelle Nom de l’établissement :
        %td.copy-btn{ role: "button" }= champ.data['nom_etablissement']
      %tr
        %th.libelle L’identifiant de l’etablissement :
        %td.copy-btn{ role: "button" }= champ.data['identifiant_de_l_etablissement']
      - if champ.data['siren_siret'].present?
        %tr
          %th.libelle SIREN/SIRET :
          %td.copy-btn{ role: "button" }= champ.data['siren_siret']
      %tr
        %th.libelle Commune :
        %td.copy-btn{ role: "button" }
          = "#{champ.data['nom_commune']}"
          = " (#{champ.data['code_commune']})" if champ.data['code_commune'].present?
      %tr
        %th.libelle Academie :
        %td.copy-btn{ role: "button" }= "#{champ.data['libelle_academie']} (#{champ.data['code_academie']})"
      %tr
        %th.libelle Nature de l’établissement :
        %td.copy-btn{ role: "button" }= "#{champ.data['libelle_nature']} (#{champ.data['code_nature']})"
      - if champ.data['type_contrat_prive'] != 'SANS OBJET'
        %tr
          %th.libelle Type de contrat privé :
          %td.copy-btn{ role: "button" }= champ.data['type_contrat_prive']

      - if champ.data['nombre_d_eleves'].present?
        %tr
          %th.libelle Nombre d’élèves :
          %td.copy-btn{ role: "button" }= champ.data['nombre_d_eleves']

      %tr
        %th.libelle Adresse :
        %td.copy-btn{ role: "button" }
          - if champ.data['adresse_1'].present?
            = champ.data['adresse_1']
          %br
          = "#{champ.data['code_postal']} #{champ.data['nom_commune']}"
          %br
          = "#{champ.data['libelle_region']} (#{champ.data['code_region']})"

      - if champ.data['telephone'].present?
        %tr
          %th.libelle Téléphone :
          %td.copy-btn{ role: "button" }= champ.data['telephone']

      - if champ.data['mail'].present?
        %tr
          %th.libelle Email :
          %td.copy-btn{ role: "button" }= champ.data['mail']

      - if champ.data['web'].present?
        %tr
          %th.libelle Site internet :
          %td.copy-btn{ role: "button" }= champ.data['web']
