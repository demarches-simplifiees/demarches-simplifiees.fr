%li{ class: (active ? 'active' : nil) }
  - if notification
    %span.notifications{ 'aria-label': 'notifications' }
  = link_to(url) do
    = label
    = render partial: 'shared/badge_filter', locals: { badge: badge, filter: filter }
