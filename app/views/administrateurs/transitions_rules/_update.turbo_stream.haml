- rendered = render @transitions_rules_component

- if rendered.present?
  = turbo_stream.replace dom_id(@procedure.draft_revision, :transitions_rules) do
    - rendered
- else
  = turbo_stream.remove dom_id(@procedure.draft_revision, :transitions_rules)
